define(['baseView'],
    function (BaseView) {
        return baseCollectionView = BaseView.extend({
            initialize: function () {
                console.log("init -> baseCollectionView");
            }
        });
    });
